# Демо-пример встраивания 1Script в приложение .NET

Примером является программа, выполняющая подсчет слов в строке.
Однако,ее поведение можно переопределить с помощью скриптового модуля.

Если в рабочем каталоге программы будет лежать файл counter.os, то он будет использован как "Модуль объекта" для встроенного в программу "Счетчика слов".

В модуле counter.os могут быть определены методы:

## Функция ПриВыполненииПодсчета(ВходящаяСтрока, СтандартнаяОбработка)

Может переопределить алгоритм подсчета. На вход получает строку, введенную пользователем.
Если флаг СтандартнаяОбработка выставить в Ложь, то значение, возвращенное из функции будет использовано, как результат подсчета.

## Процедура ПриВыводеРезультата(Значение, СтандартнаяОбработка)

Может переопределить стандартное оформление вывода результата.
Если флаг СтандартнаяОбработка выставить в Ложь, то встроенно отображение результата выполнено не будет. Ответственность за вывод возлагается на метод ПриВыводеРезультата.
